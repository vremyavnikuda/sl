#ifndef HANDLE_INPUT_H
#define HANDLE_INPUT_H

#include <vector>
#include <string>

// Объявление функции handle_input
void handle_input(bool &running, int &track_index, const std::vector<std::string> &playlist);

#endif // HANDLE_INPUT_H
